const APP_CONFIG = {
  WIDTH: 900,
  HEIGHT: 600,
  MIN_WIDTH: 700,
  MIN_HEIGHT: 466,

  BROWSER_CONFIG: {
    // Enables scroll bounce (rubber banding) effect on macOS
    scrollBounce: true,
    webSecurity: false,
  },
};

module.exports = APP_CONFIG;
