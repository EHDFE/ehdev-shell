// const { promisify } = require('util');
// const fs = require('fs');
// const readFile = promisify(fs.readFile);

module.exports = async (input, opts, extraData) => {
  // let inputBuffer;
  // if (Buffer.isBuffer(input)) {
  //   inputBuffer = input;
  // } else {
  //   inputBuffer = await readFile(input);
  // }
};
