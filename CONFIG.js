const APP_CONFIG = {
  WIDTH: 900,
  HEIGHT: 600,
  MIN_WIDTH: 700,
  MIN_HEIGHT: 466,

  BROWSER_CONFIG: {
    // Disable WebGL support
    webgl: false,
    // Enables scroll bounce (rubber banding) effect on macOS
    scrollBounce: true,
  },
};

module.exports = APP_CONFIG;
